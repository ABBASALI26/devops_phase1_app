apiVersion: v1
kind: Service
metadata:
  name: devops-phase2-service
spec:
  type: NodePort
  selector:
    app: devops-phase2-app
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080